#include <stdio.h>
#include <stdint.h>
uint32_t flipBits(uint32_t num){
    return ~num;
}
int main(){
    int q;
    scanf("%d", &q);
    for (int i=0;i<q;i++){
        uint32_t n;
        scanf("%u", &n);
        printf("%u\n", flipBits(n));
    }
    return 0;
}